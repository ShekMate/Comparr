# -------- Comparr env example (no secrets) --------
# Copy to .env for local "docker run --env-file .env" usage.
# In Unraid, set these in the template UI instead.

# Core
PLEX_URL=
PLEX_TOKEN=
PLEX_LIBRARY_NAME=
EMBY_URL=
EMBY_API_KEY=
JELLYFIN_URL=
JELLYFIN_API_KEY=
ACCESS_PASSWORD=      # Optional: gate the UI if your app supports it

# APIs
OMDB_API_KEY=
TMDB_API_KEY=
RADARR_URL=
RADARR_API_KEY=
JELLYSEERR_URL=
JELLYSEERR_API_KEY=
OVERSEERR_URL=
OVERSEERR_API_KEY=

# Runtime / container
PUID=1000             # UID to run as (entrypoint uses this)
PGID=1000             # GID to run as
PORT=8000             # If your app reads a PORT env (optional)

# Notes:
# - Do NOT commit a real .env; keep this file as docs only.
# - Unraid users can ignore .env entirely and set values in the template.

# Security
TRUST_PROXY=false      # Set true only behind a trusted proxy that rewrites forwarding headers
MAX_BODY_SIZE=1048576  # Max HTTP request body size in bytes
ALLOWED_ORIGINS=       # Comma-separated hosts/origins allowed for Host/Origin validation
