## PR Rules
- Never auto-create PRs
- When asked to create a PR, use: gh pr create --draft --base dev
- Always create PRs as drafts unless explicitly told otherwise

## Git Rules
- Never commit directly to main
- Always work on the dev branch or a feature branch off dev